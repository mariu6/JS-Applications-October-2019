function extendClass(classToExtend) {
    classToExtend = Object.assign(this, species) 

    classToExtend.toSpeciesString() {
        return "I am a <species>. <toString()>"
    }
    "I am a <species>. <toString()>"
    toString() 
}
